name: Cron — Event Reminders

on:
  schedule:
    - cron: '0 10 * * 1'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Trigger cron
        run: |
          curl -sf -X GET "https://dreamteamafrica.com/api/cron/event-reminders" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -H "Content-Type: application/json" \
            --max-time 60
